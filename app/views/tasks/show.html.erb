<h2><%= @task.title %></h2>
<%= link_to "Edit task", edit_task_path %>

<p><%= @task.details %></p>
<p><%= @task.completed ? "Task completed" : "Task still open" %></p>


<%= link_to("Back to list", "/tasks") %>
